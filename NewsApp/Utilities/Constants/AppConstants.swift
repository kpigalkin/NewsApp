import Foundation

enum AppConstants {
    enum ImageSize: CGFloat {
        case smallPreview = 120
        case preview = 350
        case full = 800
    }
    
    enum DateFormat: String {
        case presentingFormat = "MMM d, h:mm a"
    }
}
